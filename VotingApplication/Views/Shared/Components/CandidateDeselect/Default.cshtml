@model FilteredUsersViewModel.UserViewModel

@if (string.IsNullOrWhiteSpace(Model.BallotName))
{
    <button class="btn btn-dark" onclick="deselectUser();" >Deselect</button>
}
else
{
    <div>
        Already Candidate for "@Model.BallotName"
    </div>
}